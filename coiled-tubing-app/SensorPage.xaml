<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="coiled_tubing_app.SensorPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:coiled_tubing_app"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0" 
                   Text="Chart Record Management" 
                   FontSize="28" 
                   FontWeight="Bold" 
                   Margin="0,0,0,20"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="1" 
                    Orientation="Horizontal" 
                    Spacing="15" 
                    Margin="0,0,0,30">
            
            <Button x:Name="AddRecordButton"
                    Content="Add Record"
                    Style="{StaticResource AccentButtonStyle}"
                    Padding="20,10"
                    Click="AddRecordButton_Click"/>

            <Button x:Name="LoadRecordButton"
                    Content="Load Record"
                    Padding="20,10"
                    Click="LoadRecordButton_Click"/>
        </StackPanel>

        <!-- Content Area -->
        <Border Grid.Row="2" 
                CornerRadius="8" 
                BorderBrush="LightGray" 
                BorderThickness="1" 
                Padding="20">
            
            <StackPanel x:Name="ContentPanel">
                <TextBlock Text="Welcome to Chart Record Management" 
                           FontSize="20" 
                           FontWeight="SemiBold" 
                           Margin="0,0,0,15"/>
                
                <TextBlock Text="You can:" 
                           FontSize="16" 
                           Margin="0,0,0,10"/>
                
                <TextBlock Text="• Create new chart records" 
                           FontSize="14" 
                           Margin="20,5,0,5"/>
                <TextBlock Text="• Save records in .fxz format" 
                           FontSize="14" 
                           Margin="20,0,0,5"/>
                <TextBlock Text="• Load existing records" 
                           FontSize="14" 
                           Margin="20,0,0,5"/>

                <TextBlock x:Name="StatusTextBlock" 
                           Text="Ready" 
                           FontSize="14" 
                           Foreground="Green" 
                           Margin="0,20,0,0"/>
            </StackPanel>
        </Border>
    </Grid>
</Page>
