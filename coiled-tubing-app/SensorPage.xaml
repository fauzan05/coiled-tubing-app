<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="coiled_tubing_app.SensorPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:coiled_tubing_app"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:lvc="using:LiveChartsCore.SkiaSharpView.WinUI"
    mc:Ignorable="d">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <StackPanel Grid.Column="0">
                <TextBlock Text="Chart Record Management" 
                           FontSize="28" 
                           FontWeight="Bold" 
                           Margin="0,0,0,5"/>
                <TextBlock x:Name="StatusTextBlock" 
                           Text="Ready" 
                           FontSize="14" 
                           Foreground="Green"/>
            </StackPanel>
            
            <!-- Back Button (Hidden by default) -->
            <Button x:Name="BackButton"
                    Grid.Column="1"
                    Content="← Back"
                    Click="BackButton_Click"
                    Padding="15,8"
                    Style="{StaticResource AccentButtonStyle}"
                    Visibility="Collapsed"/>
        </Grid>

        <!-- Action Buttons -->
        <StackPanel Grid.Row="1" 
                    x:Name="ActionButtonsPanel"
                    Orientation="Horizontal" 
                    Spacing="15" 
                    Margin="0,0,0,20">
            
            <Button x:Name="AddRecordButton"
                    Content="+ Add Record"
                    Style="{StaticResource AccentButtonStyle}"
                    Padding="20,10"
                    Click="AddRecordButton_Click"/>

            <Button x:Name="LoadRecordButton"
                    Content="📁 Load Record"
                    Padding="20,10"
                    Click="LoadRecordButton_Click"/>
            
            <Button x:Name="RefreshHistoryButton"
                    Content="🔄 Refresh"
                    Padding="20,10"
                    Click="RefreshHistoryButton_Click"/>
                    
            <Button x:Name="ClearHistoryButton"
                    Content="🗑️ Clear All"
                    Padding="20,10"
                    Background="OrangeRed"
                    Foreground="White"
                    Click="ClearHistoryButton_Click"/>
        </StackPanel>

        <!-- Main Content Area -->
        <Grid Grid.Row="2">
            <!-- History Table View (Default) -->
            <Border x:Name="HistoryTableView"
                    CornerRadius="8" 
                    BorderBrush="LightGray" 
                    BorderThickness="1">
                
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Table Header -->
                    <Border Grid.Row="0" 
                            CornerRadius="8,8,0,0" 
                            Padding="15,12">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="60"/>
                                <ColumnDefinition Width="2*"/>
                                <ColumnDefinition Width="3*"/>
                                <ColumnDefinition Width="2*"/>
                                <ColumnDefinition Width="100"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Text="No" FontWeight="SemiBold"/>
                            <TextBlock Grid.Column="1" Text="Record Name" FontWeight="SemiBold"/>
                            <TextBlock Grid.Column="2" Text="Selected Charts" FontWeight="SemiBold"/>
                            <TextBlock Grid.Column="3" Text="Date Created" FontWeight="SemiBold"/>
                            <TextBlock Grid.Column="4" Text="Action" FontWeight="SemiBold"/>
                        </Grid>
                    </Border>

                    <!-- Table Content -->
                    <ListView x:Name="HistoryDataGrid"
                              Grid.Row="1"
                              SelectionMode="Single"
                              DoubleTapped="HistoryDataGrid_DoubleTapped"
                              BorderThickness="0">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Border BorderBrush="#E0E0E0" 
                                        BorderThickness="0,0,0,1" 
                                        Padding="15,12"
                                        Background="Transparent">
                                    <Border.Resources>
                                        <Storyboard x:Name="HoverStoryboard">
                                            <ColorAnimation Storyboard.TargetName="HoverBorder" 
                                                            Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                                            To="LightBlue" Duration="0:0:0.2"/>
                                        </Storyboard>
                                    </Border.Resources>
                                    
                                    <Grid>
                                        <Border x:Name="HoverBorder" Background="Transparent"/>
                                        
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="60"/>
                                                <ColumnDefinition Width="2*"/>
                                                <ColumnDefinition Width="3*"/>
                                                <ColumnDefinition Width="2*"/>
                                                <ColumnDefinition Width="100"/>
                                            </Grid.ColumnDefinitions>

                                            <TextBlock Grid.Column="0" 
                                                       Text="{Binding RowNumber}" 
                                                       VerticalAlignment="Center"
                                                       FontWeight="SemiBold"
                                                       Foreground="Gray"/>
                                            
                                            <TextBlock Grid.Column="1" 
                                                       Text="{Binding RecordName}" 
                                                       VerticalAlignment="Center"
                                                       FontWeight="SemiBold"
                                                       Foreground="DarkBlue"/>
                                            
                                            <TextBlock Grid.Column="2" 
                                                       Text="{Binding ChartsPreview}" 
                                                       VerticalAlignment="Center"
                                                       TextWrapping="Wrap"
                                                       FontSize="13"/>
                                            
                                            <TextBlock Grid.Column="3" 
                                                       Text="{Binding FormattedDate}" 
                                                       VerticalAlignment="Center"
                                                       FontSize="13"
                                                       Foreground="Gray"/>
                                            
                                            <Button Grid.Column="4"
                                                    Content="❌"
                                                    Background="Transparent"
                                                    BorderBrush="Red"
                                                    Foreground="Red"
                                                    FontSize="12"
                                                    Padding="8,4"
                                                    Tag="{Binding}"
                                                    Click="DeleteHistoryItem_Click"
                                                    ToolTipService.ToolTip="Delete this record"/>
                                        </Grid>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </Grid>
            </Border>

            <!-- Chart TabView (Hidden by default) -->
            <Border x:Name="ChartTabView"
                    CornerRadius="8" 
                    BorderBrush="LightGray" 
                    BorderThickness="1" 
                    Padding="10"
                    Visibility="Collapsed">
                
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Record Title -->
                    <TextBlock x:Name="RecordTitleTextBlock"
                               Grid.Row="0"
                               Text=""
                               FontSize="20"
                               FontWeight="Bold"
                               Margin="10,0,0,15"
                               Foreground="DarkBlue"/>

                    <!-- TabView for Charts -->
                    <TabView x:Name="ChartsTabView"
                             Grid.Row="1"
                             TabWidthMode="Equal"
                             CanDragTabs="False"
                             CanReorderTabs="False"
                             IsAddTabButtonVisible="False">
                        <!-- Tabs will be created dynamically in code-behind -->
                    </TabView>
                </Grid>
            </Border>
        </Grid>

        <!-- Status Bar -->
        <Border Grid.Row="3" 
                CornerRadius="4" 
                Padding="15,8" 
                Margin="0,20,0,0"
                BorderBrush="LightGray"
                BorderThickness="1">
            <TextBlock x:Name="HistoryStatusTextBlock"
                       Text="No files in history"
                       FontSize="12"
                       Foreground="Gray"
                       FontWeight="SemiBold"/>
        </Border>
    </Grid>
</Page>
